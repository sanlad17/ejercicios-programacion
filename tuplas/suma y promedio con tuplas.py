def suma_promedio_tupla():
    numeros = (10, 20, 30, 40)
    suma = sum(numeros)
    promedio = suma / len(numeros)
    print(f'suma = {suma}')
    print(f'promedio = {promedio}')
suma_promedio_tupla()
